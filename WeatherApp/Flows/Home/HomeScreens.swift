enum HomeScreens: Hashable {
    case splash
    case current
    case forecast
    case lostConnect
}

enum Home {
    enum Splash {}
    enum Current {}
    enum Forecast {}
    enum LostConnect {}
}
